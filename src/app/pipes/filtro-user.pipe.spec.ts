import { FiltroUserPipe } from './filtro-user.pipe';

describe('FiltroUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroUserPipe();
    expect(pipe).toBeTruthy();
  });
});
